
public class Arrays { 
    public static void main(String[] args) {
        // Scanner in = new Scanner(System.in);
        // int[][] arr = new int[3][3];
        // for (int row = 0; row < arr.length; row++) { 
        //     for (int col = 0; col < arr[row].length; col++) {
        //      arr[row][col]= in.nextInt() ;  
        //     }}
        // // for (int[] arr1 : arr) {
        // //     for (int col = 0; col < arr1.length; col++) {
        // //         System.out.print(arr1[col] + " ");
        // //     }
        // //     System.out.println(" ");
        // // }            
        // for(int[] a : arr){
        //     System.out.println(Arrays.toString(a));
        }

        // ArrayList<Integer> list = new ArrayList<>();       // Array list (1D)

        // list.add(599999);
        // list.add(5777);
        // list.add(57);
        // list.add(599);
        // list.add(59);
        // System.out.println(list);
        // list.remove(2);
        // System.out.println(list);

        // for (int i = 0; i < 5; i++) {   // taking input in the Array list
        //  list.add(in.nextInt()) ;  
        // }

        
        //    System.out.println(list);      // Printing the list
           

        // ArrayList<ArrayList<Integer>> list = new ArrayList<>();           // syntax of 2D Array list
        // for (int i = 0; i < 3; i++) {  // Intialisation
        //     list.add(new ArrayList<>());
        // }
        // for (int i = 0; i < 3; i++) {   // Adding of element
        //    for (int j = 0; j < 3; j++) {
        //     list.get(i).add(in.nextInt()) ; 
        //    } 
        // }
        // System.out.println(list);  // Printing the 2D Array list
        
  //       float i =7;
  // System.out.println(i/3);
  
          // System.out.println(Math.pow(5, 3));

        // int[] arr1= {1,2,3,4,5};
        // int[] arr2= {1,2,3};            // comparing two arrays when its different guud 
        // System.out.println(arr1==arr2);   // false 

        // int[] arr3= {1,2,3};
        // int[] arr4= {1,2,3};            // comparing two arrays if its same.but ouput is False.
        // System.out.println(arr3==arr4); // because its arr1 and arr2 are reference to the different objects in heap memory.

          // now to compare properly we uses .equals() method for arrays 
          // System.out.println(java.util.Arrays.equals(arr3, arr4));  // now its showing True...
     
          
}      
  }